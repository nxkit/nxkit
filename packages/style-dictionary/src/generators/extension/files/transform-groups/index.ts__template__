import {
  CustomTransformGroupsBuilder,
  ExtensionContext,
} from '@nxkit/style-dictionary';

const customTransformGroupsBuilder: CustomTransformGroupsBuilder = (
  extensionContext: ExtensionContext
) => {
  return {
    MyCustomGroup: ['attribute/cti', 'color/rgb', 'name/cti/pascal'],
  };
};

export default customTransformGroupsBuilder;
